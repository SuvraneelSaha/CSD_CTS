~~~
/*
	 * Create an interface Validator
	 * create a method validate as follows
	 * 
	 * public boolean validate(String str);
	 * 
	 * 
	 * 1.check whether the given string is valid cohortCode
	 * CSDCIS24CS001
	 * 
	 * 2. Check whether the given string is valid email 
	 */
  /*
	 * Create an interface Validator
	 * create a method validate as follows
	 * 
	 * public boolean validate(String str);
	 * 
	 * 
	 * 1.check whether the given string is valid cohortCode
	 * CSDCIS24CS001
	 * 
	 * 2. Check whether the given string is valid email 
	 * 
	 * 1. should contain @
	 * 2. ends with .com
	 */
   ~~~
   